DB_NAME=v17
ADDONS_PATH=./addons # This is the path if addons are in the odoo/addons folder
# ADDONS_PATH=../addons # This is the path if addons are in the odoo parent folder
ENTERPRISE_PATH=./enterprise # Set this to the path of the enterprise addons if you have them
ODOO_ARGS=--limit-time-real 0 -u all